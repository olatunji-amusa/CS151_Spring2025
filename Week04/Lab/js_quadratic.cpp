#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int a, b, c;
    double x_1, x_2;

    cout << "Enter values for a, b, and c: ";
    cin >> a >> b >> c;

    int exp = pow(b,2) - 4 * a * c;
    bool check; 

    if (check)
    {
        return 1;
    }



    return 0;
}